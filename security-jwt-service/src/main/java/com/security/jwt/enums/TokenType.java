package com.security.jwt.enums;

import com.security.jwt.model.JwtClientDetails;

/**
 * Token types managed by the security microservice and configured in {@link JwtClientDetails}
 */
public enum TokenType {
    Bearer
}
